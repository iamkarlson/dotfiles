################################################################################
# Keybindings for native hyprland stuff like plugins that do not use `exec`
################################################################################

bind = SUPER, g, hyprexpo:expo, toggle

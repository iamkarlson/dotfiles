# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: ":acc"
    replace: |-
      **Acceptance criteria**:
      - [ ]
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":tomorrow"
    replace: "{{mytime}}"
    vars:
      - name: mytime
        type: date
        params:
          format: "%Y-%m-%d"
          offset: 86400
  - trigger: ":week"
    replace: "{{week_number}}"
    vars:
      - name: week_number
        type: date
        params:
          format: "%U"
  - trigger: ":timenow"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d %H:%M:%S"
  - trigger: m^2
    replace: m²
  - trigger: ":yes"
    replace: ✓
  - trigger: georgy@dexter
    replace: georgy@dexterenergy.nl
  - trigger: ":lbl"
    replace: /label ~
  - trigger: ":apiurl"
    replace: "https://api.dexterenergyservices.com"
  - trigger: /assign
    replace: /assign_reviewer @joao117 @nikita.dexter @patrick_star_21
  - trigger: m2m
    replace: many-to-many
  - trigger: /gihpy
    replace: /giphy
  - trigger: ":password"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "< /dev/urandom tr -dc A-Za-z0-9 | head -c14; echo"
  - trigger: ":ip"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "curl 'https://api.ipify.org'"